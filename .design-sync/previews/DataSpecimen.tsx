// Preview card for DataSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/DataSpecimen.tsx (single source).
import { DataSpecimen } from 'gst-website';

export const Gallery = () => <DataSpecimen />;
