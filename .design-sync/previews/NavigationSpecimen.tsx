// Preview card for NavigationSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/NavigationSpecimen.tsx (single source).
import { NavigationSpecimen } from 'gst-website';

export const Gallery = () => <NavigationSpecimen />;
