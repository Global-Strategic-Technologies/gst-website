// Preview card for FrostedSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/FrostedSpecimen.tsx (single source).
import { FrostedSpecimen } from 'gst-website';

export const Gallery = () => <FrostedSpecimen />;
