// Preview card for FormSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/FormSpecimen.tsx (single source).
import { FormSpecimen } from 'gst-website';

export const Gallery = () => <FormSpecimen />;
