// Preview card for TypographySpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/TypographySpecimen.tsx (single source).
import { TypographySpecimen } from 'gst-website';

export const Gallery = () => <TypographySpecimen />;
