// Preview card for CardSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/CardSpecimen.tsx (single source).
import { CardSpecimen } from 'gst-website';

export const Gallery = () => <CardSpecimen />;
