// Preview card for ToolShellSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/ToolShellSpecimen.tsx (single source).
import { ToolShellSpecimen } from 'gst-website';

export const Gallery = () => <ToolShellSpecimen />;
