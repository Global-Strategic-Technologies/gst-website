// Preview card for ColorSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/ColorSpecimen.tsx (single source).
import { ColorSpecimen } from 'gst-website';

export const Gallery = () => <ColorSpecimen />;
