// Preview card for ToolChromeSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/ToolChromeSpecimen.tsx (single source).
import { ToolChromeSpecimen } from 'gst-website';

export const Gallery = () => <ToolChromeSpecimen />;
