// Preview card for ButtonSpecimen — renders the real bundle export.
// Markup lives in .design-sync/specimens/ButtonSpecimen.tsx (single source).
import { ButtonSpecimen } from 'gst-website';

export const Gallery = () => <ButtonSpecimen />;
