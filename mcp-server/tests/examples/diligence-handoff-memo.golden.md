---
promptName: gst_diligence_handoff_memo
version: 0.1.0
recordedAt: TBD
model: TBD
---

# Worked example output for `gst_diligence_handoff_memo`

> **Status**: placeholder — V8 recorded output will be captured here during senior-consultant review (see BL-031.75 verification punch-list V8).

## Input

TBD — the full `UserInputsSchema` + `targetName` payload used for the V8 invocation. Optional `agendaJson` / `comparablesJson` may be supplied.

## Expanded prompt body

TBD — the output of `diligenceHandoffMemoPrompt.build(args)`.

## Model output

TBD — single coherent handoff memo (engagement context, agenda, attention areas cross-referenced to comparables, comparable engagement library, prioritized VDR follow-ups, open questions / next steps).
