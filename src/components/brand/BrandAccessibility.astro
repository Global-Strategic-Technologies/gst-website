---
import { responsiveFramePath } from '../../utils/responsive-demo-groups';

/**
 * The Responsive Behavior rows. Driven by data rather than twelve near-identical
 * markup blocks so a group cannot be added to the route without appearing here —
 * BL-097 shipped with all twelve frames rendering the same group precisely
 * because each `src` was written out by hand.
 *
 * `group` is validated against RESPONSIVE_DEMO_GROUPS by `responsiveFramePath`'s
 * parameter type, so a typo is a build error rather than a silent 404.
 */
const RESPONSIVE_ROWS = [
  { group: 'cards', id: 'resp-option-card', heading: 'Option Card Grid', label: 'Cards' },
  { group: 'tabs', id: 'resp-tab-bar', heading: 'Tab Bar', label: 'Tabs' },
  { group: 'form', id: 'resp-form-controls', heading: 'Form Controls', label: 'Form' },
  { group: 'shell', id: 'resp-tool-shell', heading: 'Tool Shell', label: 'Shell' },
] as const;

/** Frame sizes are set by these modifier classes — see brand.astro's global block. */
const RESPONSIVE_WIDTHS = [
  { modifier: 'desktop', label: '1200px' },
  { modifier: 'tablet', label: '768px' },
  { modifier: 'mobile', label: '480px' },
] as const;
---

<!-- Section 7c: Accessibility -->
<section class="brand-section" id="accessibility">
  <div class="container">
    <h2 class="brutal-heading-md brand-section__title">Accessibility</h2>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-xl); color: var(--text-muted);"
    >
      WCAG 2.1 AA compliance reference — with touch targets held to the stricter AAA 2.5.5 figure.
      Focus states, contrast ratios, touch targets, keyboard patterns, and semantic structure.
    </p>

    <!-- A: Focus States -->
    <h3 class="brutal-heading-sm brand-group-title" id="a11y-focus">Focus States</h3>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-md); color: var(--text-muted);"
    >
      All interactive components use a <code class="brand-code">2px solid var(--color-primary)</code
      > outline with <code class="brand-code">2px offset</code> on <code class="brand-code"
        >:focus-visible</code
      >.
    </p>
    <div class="brand-state-row">
      <div class="brand-state-item">
        <button class="brutal-btn brutal-btn--primary" data-demo-state="focus">Button</button>
        <code class="brand-code">:focus-visible</code>
      </div>
      <div class="brand-state-item">
        <div class="brutal-field" style="margin-bottom: 0;">
          <!-- No visible label in this specimen (it demonstrates the focus ring,
               not the field pattern), so the accessible name comes from aria-label. -->
          <input
            class="brutal-field__input"
            type="text"
            value="Input field"
            data-demo-state="focus"
            aria-label="Focus state demo field"
          />
        </div>
        <code class="brand-code">:focus (solid border)</code>
      </div>
      <div class="brand-state-item">
        <div class="brutal-option-card" data-demo-state="focus" tabindex="0">
          <span class="brutal-option-card__label">Option Card</span>
        </div>
        <code class="brand-code">:focus-visible</code>
      </div>
      <div class="brand-state-item">
        <button class="brutal-choice-btn" type="button" data-demo-state="focus">Choice</button>
        <code class="brand-code">:focus-visible</code>
      </div>
      <div class="brand-state-item">
        <a class="brutal-link-interactive" href="#accessibility" data-demo-state="focus">Link</a>
        <code class="brand-code">:focus-visible</code>
      </div>
    </div>

    <!-- A: Contrast Ratio Table -->
    <h3 class="brutal-heading-sm brand-group-title" id="a11y-contrast">Color Contrast Ratios</h3>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-md); color: var(--text-muted);"
    >
      Text hierarchy on page backgrounds. WCAG 2.1 AA requires 4.5:1 for normal text, 3:1 for large
      text (&ge;18px or &ge;14px bold); AAA requires 7:1 for normal text.
    </p>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-md); color: var(--text-muted);"
    >
      Every swatch renders the live token, never a copy of its value. The per-theme rows use the
      static constants <code class="brand-code">--text-light-*</code> / <code class="brand-code"
        >--text-dark-*</code
      > that <code class="brand-code">--text-*</code> resolves to in each theme, so both tables stay accurate
      whichever theme you are viewing. <code class="brand-code">--color-primary</code> is palette-driven
      — switch palettes and these two rows follow.
    </p>

    <h4 class="brutal-label brand-subgroup-title">Light Theme (on #ffffff)</h4>
    <div class="a11y-contrast-table">
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--text-light-primary);"></span>
        <span class="a11y-var">--text-primary</span>
        <span class="a11y-ratio">~15.2:1</span>
        <span class="a11y-badge a11y-badge--pass">AAA</span>
      </div>
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--text-light-secondary);"></span>
        <span class="a11y-var">--text-secondary</span>
        <span class="a11y-ratio">~6.4:1</span>
        <span class="a11y-badge a11y-badge--pass">AA</span>
      </div>
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--text-light-muted);"></span>
        <span class="a11y-var">--text-muted</span>
        <span class="a11y-ratio">~5.4:1</span>
        <span class="a11y-badge a11y-badge--pass">AA</span>
      </div>
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--color-primary);"></span>
        <span class="a11y-var">--color-primary</span>
        <span class="a11y-ratio">~2.1:1 <em>(default palette)</em></span>
        <span class="a11y-badge a11y-badge--fail">Decorative only</span>
      </div>
    </div>

    <h4 class="brutal-label brand-subgroup-title">Dark Theme (on #0a0a0a)</h4>
    <div class="a11y-contrast-table">
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--text-dark-primary);"></span>
        <span class="a11y-var">--text-primary</span>
        <span class="a11y-ratio">~16.3:1</span>
        <span class="a11y-badge a11y-badge--pass">AAA</span>
      </div>
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--text-dark-secondary);"></span>
        <span class="a11y-var">--text-secondary</span>
        <span class="a11y-ratio">~7.8:1</span>
        <span class="a11y-badge a11y-badge--pass">AAA</span>
      </div>
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--text-dark-muted);"></span>
        <span class="a11y-var">--text-muted</span>
        <span class="a11y-ratio">~4.7:1</span>
        <span class="a11y-badge a11y-badge--pass">AA</span>
      </div>
      <div class="a11y-contrast-row">
        <span class="a11y-swatch" style="background: var(--color-primary);"></span>
        <span class="a11y-var">--color-primary</span>
        <span class="a11y-ratio">~9.6:1 <em>(default palette)</em></span>
        <span class="a11y-badge a11y-badge--pass">AAA</span>
      </div>
    </div>

    <!-- B: Touch Targets -->
    <h3 class="brutal-heading-sm brand-group-title" id="a11y-touch">Touch Targets</h3>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-md); color: var(--text-muted);"
    >
      WCAG 2.5.5 (Level AAA) asks for a 44&times;44px minimum — stricter than the 24&times;24px of
      AA 2.5.8. <code class="brand-code">.brutal-btn</code>, <code class="brand-code"
        >.brutal-choice-btn</code
      > (including <code class="brand-code">--unsure</code>) take the shared <code
        class="brand-code">--touch-target-min</code
      > floor directly, as do <code class="brand-code">.filter-button</code>, <code
        class="brand-code">.modal-close</code
      > and <code class="brand-code">.theme-toggle</code>; <code class="brand-code"
        >.cta-button</code
      > clears the same figure from its own padding above 480px and the token below it. BL-096 settled
      the scope: 44px is <strong>guaranteed and enforced on those families</strong>, and everywhere
      else the bar is AA 2.5.8 (24&times;24) via <code class="brand-code"
        >--touch-target-min-aa</code
      >, now <strong>enforced by axe's <code class="brand-code">target-size</code></strong> on every route
      the accessibility suite scans rather than asserted. It is measured on the <em>target</em>, so
      a small control inside a clickable label already passes. The two map zoom clusters are
      recorded deviations — they meet AA, not AAA. The dashed outline below is the 44&times;44
      minimum drawn over each control.
    </p>
    <div class="brand-state-row">
      <div class="brand-state-item">
        <div class="a11y-touch-demo">
          <button class="brutal-btn brutal-btn--primary">Button</button>
          <div class="a11y-touch-overlay" aria-hidden="true"></div>
        </div>
        <code class="brand-code">.brutal-btn — 44&times;44 minimum</code>
      </div>
      <div class="brand-state-item">
        <div class="a11y-touch-demo">
          <button class="brutal-choice-btn" type="button">Choice</button>
          <div class="a11y-touch-overlay" aria-hidden="true"></div>
        </div>
        <code class="brand-code">.brutal-choice-btn — 44&times;44 minimum</code>
      </div>
    </div>

    <!-- B: Keyboard Navigation -->
    <h3 class="brutal-heading-sm brand-group-title" id="a11y-keyboard">Keyboard Navigation</h3>
    <div class="a11y-kb-table">
      <div class="a11y-kb-row a11y-kb-row--header">
        <span>Pattern</span>
        <span>Key</span>
        <span>Behavior</span>
      </div>
      <div class="a11y-kb-row">
        <span>Sequential focus</span>
        <kbd>Tab</kbd>
        <span>Move to next focusable element</span>
      </div>
      <div class="a11y-kb-row">
        <span>Reverse focus</span>
        <kbd>Shift+Tab</kbd>
        <span>Move to previous focusable element</span>
      </div>
      <div class="a11y-kb-row">
        <span>Activate</span>
        <kbd>Enter / Space</kbd>
        <span>Trigger button, select option card</span>
      </div>
      <div class="a11y-kb-row">
        <span>Tab bar</span>
        <kbd>&larr; &rarr;</kbd>
        <span>Navigate between tabs (when focused)</span>
      </div>
      <div class="a11y-kb-row">
        <span>Modal dismiss</span>
        <kbd>Escape</kbd>
        <span>Close modal, return focus to trigger</span>
      </div>
      <div class="a11y-kb-row">
        <span>Modal trap</span>
        <kbd>Tab (in modal)</kbd>
        <span>Focus cycles within modal boundaries</span>
      </div>
    </div>

    <!-- B: ARIA Patterns -->
    <h3 class="brutal-heading-sm brand-group-title" id="a11y-aria">ARIA Patterns</h3>
    <div class="a11y-code-block">
      <pre><code>&lt;!-- Live region for dynamic results --&gt;
&lt;div aria-live="polite" aria-atomic="true"&gt;
  3 regulations found
&lt;/div&gt;

&lt;!-- Labeled input --&gt;
&lt;label class="brutal-field__label" for="company"&gt;Company&lt;/label&gt;
&lt;input class="brutal-field__input" id="company" aria-required="true" /&gt;

&lt;!-- Tab bar --&gt;
&lt;div role="tablist"&gt;
  &lt;button role="tab" aria-selected="true"&gt;Active&lt;/button&gt;
  &lt;button role="tab" aria-selected="false"&gt;Inactive&lt;/button&gt;
&lt;/div&gt;

&lt;!-- Modal with focus trap --&gt;
&lt;div role="dialog" aria-modal="true" aria-labelledby="modal-title"&gt;
  &lt;h2 id="modal-title"&gt;Project Details&lt;/h2&gt;
&lt;/div&gt;</code></pre>
    </div>

    <!-- B: Semantic HTML -->
    <h3 class="brutal-heading-sm brand-group-title" id="a11y-semantic">Semantic HTML Structure</h3>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-md); color: var(--text-muted);"
    >
      Heading hierarchy and landmark roles used across the site.
    </p>
    <div class="a11y-code-block">
      <pre><code>&lt;body&gt;
  &lt;header role="banner"&gt;         &lt;!-- .site-header — sticky nav --&gt;
    &lt;nav role="navigation"&gt;...&lt;/nav&gt;
  &lt;/header&gt;
  &lt;main role="main"&gt;             &lt;!-- page content --&gt;
    &lt;h1&gt;Page Title&lt;/h1&gt;
    &lt;section&gt;
      &lt;h2&gt;Section&lt;/h2&gt;
      &lt;h3&gt;Subsection&lt;/h3&gt;
    &lt;/section&gt;
  &lt;/main&gt;
  &lt;footer role="contentinfo"&gt;    &lt;!-- site footer --&gt;
    ...
  &lt;/footer&gt;
&lt;/body&gt;</code></pre>
    </div>
  </div>
</section>

<!-- Section 7d: Responsive Behavior -->
<section class="brand-section" id="responsive-demos">
  <div class="container">
    <h2 class="brutal-heading-md brand-section__title">Responsive Behavior</h2>
    <p
      class="brutal-text-small"
      style="margin-bottom: var(--spacing-xl); color: var(--text-muted);"
    >
      Components rendered at desktop (1200px), tablet (768px), and mobile (480px) widths. Each frame
      is a live embed showing how layouts collapse and reflow.
    </p>

    {
      /* tabindex on the scroll container: the row is `overflow-x: auto`, so without
         it a keyboard user cannot scroll to the frames past the viewport edge. */
      RESPONSIVE_ROWS.map((row) => (
        <>
          <h3 class="brutal-heading-sm brand-group-title" id={row.id}>
            {row.heading}
          </h3>
          <div class="responsive-demo-row" tabindex="0" role="group" aria-labelledby={row.id}>
            {RESPONSIVE_WIDTHS.map((width) => (
              <div class={`responsive-demo-frame responsive-demo-frame--${width.modifier}`}>
                <span class="responsive-demo-label">{width.label}</span>
                <iframe
                  src={responsiveFramePath(row.group)}
                  title={`${row.label} at ${width.label}`}
                  loading="lazy"
                  style="overflow:hidden"
                />
              </div>
            ))}
          </div>
        </>
      ))
    }
  </div>
</section>
