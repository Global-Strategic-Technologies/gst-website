---
// English route for /terms/. Body: src/page-templates/TermsPage.astro,
// shared with every locale via src/pages/[locale]/[...route].astro (BL-153).
import TermsPage from '../page-templates/TermsPage.astro';
import { DEFAULT_LOCALE } from '../i18n/locales';
---

<TermsPage locale={DEFAULT_LOCALE} />
