---
// English route for /hub/mcp/. Body: src/page-templates/HubMcpPage.astro,
// shared with every locale via src/pages/[locale]/[...route].astro (BL-153).
// `tests/integration/mcp-marketing-parity.test.ts` reads the TEMPLATE's markup
// (with the English catalog inlined) against mcp-server source.
import HubMcpPage from '../../../page-templates/HubMcpPage.astro';
import { DEFAULT_LOCALE } from '../../../i18n/locales';
---

<HubMcpPage locale={DEFAULT_LOCALE} />
