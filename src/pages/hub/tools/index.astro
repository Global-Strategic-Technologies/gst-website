---
// English route for /hub/tools/. Body: src/page-templates/HubToolsPage.astro,
// shared with every locale via src/pages/[locale]/[...route].astro (BL-153).
// The ItemList JSON-LD lives in the template; tests/unit/hub-tool-schema.test.ts
// reads it there.
import HubToolsPage from '../../../page-templates/HubToolsPage.astro';
import { DEFAULT_LOCALE } from '../../../i18n/locales';
---

<HubToolsPage locale={DEFAULT_LOCALE} />
