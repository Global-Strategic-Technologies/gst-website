---
// English route for /hub/. Body: src/page-templates/HubPage.astro, shared with
// every locale via src/pages/[locale]/[...route].astro (BL-153). This file is
// the model page CLAUDE.md points new pages at; the composition it used to
// show (BaseLayout + Hero + cards + FAQ + CTASection) now lives in the template.
import HubPage from '../../page-templates/HubPage.astro';
import { DEFAULT_LOCALE } from '../../i18n/locales';
---

<HubPage locale={DEFAULT_LOCALE} />
