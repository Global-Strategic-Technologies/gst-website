---
// English route for /. Body: src/page-templates/HomePage.astro, shared with
// every locale via src/pages/[locale]/[...route].astro (BL-153).
import HomePage from '../page-templates/HomePage.astro';
import { DEFAULT_LOCALE } from '../i18n/locales';
---

<HomePage locale={DEFAULT_LOCALE} />
