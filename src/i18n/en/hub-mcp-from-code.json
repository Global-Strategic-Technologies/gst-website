{
  "meta.title": "Use the GST MCP Server from code | GST",
  "meta.description": "Call the GST MCP server from a script or pipeline with OAuth 2.1 client credentials: exchange a client ID and secret for an hourly bearer token, then send JSON-RPC to /mcp.",
  "og.title": "Use the GST MCP Server from code",
  "og.description": "Two HTTP calls between a client ID and secret and a working tool call: the token exchange, the authenticated request, and the hourly re-exchange.",
  "og.imageAlt": "GST MCP Server - developer integration guide",
  "label": "Integration guide",
  "title": "Use the GST MCP Server from code",
  "subtitle": "Two HTTP calls between a client ID and secret and a working tool call. This page is for code you run: a script, a pipeline, an agent framework. To use the server inside Claude, follow the connector guide instead.",
  "checkpoint.kicker1": "Checkpoint 01",
  "checkpoint.title1": "Credential in hand",
  "checkpoint.desc1": "A client ID and secret, issued by the free trial or provisioned by GST.",
  "checkpoint.kicker2": "Checkpoint 02",
  "checkpoint.title2": "Token in hand",
  "checkpoint.desc2": "The token endpoint returned an mcp_m2m_ access token.",
  "checkpoint.kicker3": "Checkpoint 03",
  "checkpoint.title3": "First call answered",
  "checkpoint.desc3": "A tools/list request came back with the tool roster.",
  "toc.prerequisites": "01 Before you start",
  "toc.token": "02 Get a token",
  "toc.call": "03 Call the server",
  "toc.lifecycle": "04 Keep it running",
  "toc.next": "05 Where to go next",
  "copy": "Copy",
  "copied": "Copied",
  "prereq.heading": "01 Before you start",
  "prereq.intro": "You need a client ID and a client secret. The free 3-day trial issues a pair in one click; anything wider is provisioned by GST. There is no dynamic client registration.",
  "prereq.trialCta": "Start a free trial",
  "prereq.accessCta": "Request access",
  "prereq.what.title": "What the credential is",
  "prereq.what.body": "An OAuth 2.1 <code>client_credentials</code> client. The secret is shown once when it is issued and is bearer-equivalent: keep both values in a secret store or in environment variables, never in a committed file or a shell command.",
  "prereq.not.title": "What it is not",
  "prereq.not.body": "It does not drive Claude, Cursor or ChatGPT. Those clients authenticate a person through the consent page with a trial key or an operator-issued key, and there is no config-file or custom-header way to make them use this credential. For that path, use the connector guide.",
  "prereq.not.cta": "Open the connector guide",
  "prereq.auth.title": "Three ways to authenticate at the token endpoint",
  "prereq.auth.body": "Send <code>client_id</code> and <code>client_secret</code> in the form body, as shown below, or as HTTP Basic (<code>client_secret_basic</code>). A provisioned client can register an ES256 public key and use <code>private_key_jwt</code> instead, so no shared secret leaves your infrastructure; ask GST when the client is created.",
  "token.heading": "02 Exchange the credential for a token",
  "token.intro": "POST a form-encoded body to the token endpoint. Use this URL directly: the server's OAuth discovery document describes the connector flow and does not list <code>client_credentials</code>, so do not rely on a library to find it.",
  "token.endpointLabel": "Token endpoint",
  "token.request.title": "Request",
  "token.request.copy": "Copy the token request",
  "token.response.title": "Response",
  "token.response.body": "<code>access_token</code> is the bearer you send on every call. <code>expires_in</code> is in seconds: 3600, one hour. <code>token_type</code> is lowercase <code>bearer</code> on the wire. There is no refresh token in this response, and there will not be one.",
  "token.expired.title": "401 invalid_client: Client credentials have expired",
  "token.expired.body": "The client record has lapsed. For a trial that means the 72 hours are over; signing up again on the same network replaces the pair. Any other 401 at this step means the ID or secret is wrong.",
  "call.heading": "03 Call the server",
  "call.intro": "Send JSON-RPC 2.0 to <code>/mcp</code> with the token in <code>Authorization: Bearer</code>. The <code>Accept</code> header must list both <code>application/json</code> and <code>text/event-stream</code>: the transport is Streamable HTTP and rejects a request without it.",
  "call.request.title": "A smoke test: list the tools",
  "call.request.copy": "Copy the tools/list call",
  "call.result.body": "Expect a <code>result.tools</code> array naming the sixteen diligence, portfolio and regulatory tools. Call one with <code>tools/call</code> and its arguments; every contract is in the capability reference.",
  "call.result.cta": "Open the capability reference",
  "call.sdk.title": "Using an MCP SDK",
  "call.sdk.body": "The official MCP SDKs speak this transport natively: point the Streamable HTTP client at the endpoint and give it the client ID and secret through the SDK's ClientCredentialsProvider. The SDK then performs the exchange above and repeats it when the token expires.",
  "call.radar.title": "Radar tools are refused on a trial",
  "call.radar.body": "A trial credential cannot call the radar tools. The call returns JSON-RPC error <code>-32002</code> inside an HTTP 200, not a transport failure. Radar is granted separately with a provisioned client.",
  "lifecycle.heading": "04 Keep it running",
  "lifecycle.hourly.title": "The token expires every hour",
  "lifecycle.hourly.body": "The token is not the credential. It lasts one hour and there is no refresh token: when a call returns 401, repeat step 02 and continue. Store the client ID and secret, mint tokens on demand, and never persist a token as if it were the key. A 401 from the token endpoint itself means the credential has expired.",
  "lifecycle.ceilings.title": "Trial ceilings",
  "lifecycle.ceilings.body": "A trial credential works for 72 hours at 15 calls per minute and 100 per day. Non-contractual capacity limits, not an SLA. A 429 carries a Retry-After header; back off for that long rather than retrying immediately.",
  "lifecycle.after.title": "After the trial ends",
  "lifecycle.after.body": "The token exchange and every call return 401. Delete the pair from your secret store. For continued access, request an evaluation licence: a provisioned client keeps the same two-step flow with wider scopes and higher ceilings.",
  "next.heading": "05 Where to go next",
  "next.intro": "Three routes on from a working call: every tool's contract, the interactive path, and a wider grant.",
  "next.docs.title": "Capability reference",
  "next.docs.blurb": "Arguments, an example call and what comes back, for every tool, prompt and resource.",
  "next.docs.cta": "Read docs",
  "next.connector.title": "Use it from Claude",
  "next.connector.blurb": "The connector flow for Claude on the web and Claude Desktop.",
  "next.connector.cta": "Open guide",
  "next.access.title": "Widen your access",
  "next.access.blurb": "Add radar scope, raise a tier, or provision a client with private_key_jwt.",
  "next.access.cta": "Contact GST"
}
